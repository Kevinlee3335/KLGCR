import { CalendarClock } from "lucide-react"
import { PlaceholderPage } from "@/components/dashboard-bits"

export default function Page() {
  return (
    <PlaceholderPage
      icon={CalendarClock}
      title="Appointments"
      description="Appointment scheduling will be available in a later phase."
    />
  )
}
