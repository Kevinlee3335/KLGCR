import { ListChecks } from "lucide-react"
import { PlaceholderPage } from "@/components/dashboard-bits"

export default function Page() {
  return (
    <PlaceholderPage
      icon={ListChecks}
      title="Daily Tasks"
      description="Daily task scheduling will be available in a later phase."
    />
  )
}
