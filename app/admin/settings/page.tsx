import { Settings } from "lucide-react"
import { PlaceholderPage } from "@/components/dashboard-bits"

export default function Page() {
  return (
    <PlaceholderPage
      icon={Settings}
      title="Settings"
      description="Application settings will be available in a later phase."
    />
  )
}
