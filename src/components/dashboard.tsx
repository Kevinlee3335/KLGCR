export function Dashboard({kind,name,blocks,values}:{kind:"admin"|"staff";name:string;blocks?:string;values:number[]}){const labels=kind==="admin"?[["New complaints","Awaiting review"],["Today's tasks","Assigned or active today"],["Completed today","Live completion count"],["Outstanding","Active carry-forward work"]]:[["To do","Assigned work"],["In progress","Active work"],["Pending material","Awaiting stock"],["Completed","All completed"]];return <><section className="welcome"><div><p className="eyebrow">{kind==="admin"?"Operations overview":`My tasks — ${blocks||"assigned blocks"}`}</p><h2>Good day, {name.split(" ")[0]}</h2><p className="subtle">{kind==="admin"?"Monitor maintenance activity across the residence.":"Open an assigned job to begin work."}</p></div><span className="date-badge">Phase 2 · Live workflow</span></section><section className="metrics">{labels.map(([label,hint],i)=><article className="panel metric" key={label}><span className="subtle">{label}</span><div className="value">{values[i]||0}</div><small>{hint}</small></article>)}</section></>}
