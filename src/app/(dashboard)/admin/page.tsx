import { requireRole } from "@/lib/auth"; import { AppShell } from "@/components/app-shell"; import { Dashboard } from "@/components/dashboard";import {createClient} from "@/lib/supabase/server";
export default async function AdminPage(){const profile=await requireRole(["admin","management_viewer"]),s=await createClient(),today=new Date().toISOString().slice(0,10);const [{count:a},{count:b},{count:c},{count:d}]=await Promise.all([s.from("complaints").select("*",{count:"exact",head:true}).eq("status","new"),s.from("maintenance_jobs").select("*",{count:"exact",head:true}).gte("assigned_at",today).not("status","in","(completed,cancelled)"),s.from("maintenance_jobs").select("*",{count:"exact",head:true}).eq("status","completed").gte("completed_at",today),s.from("maintenance_jobs").select("*",{count:"exact",head:true}).not("status","in","(completed,cancelled)")]);return <AppShell profile={profile} title="Admin Dashboard"><Dashboard kind="admin" name={profile.full_name} values={[a||0,b||0,c||0,d||0]}/></AppShell>}
