/* eslint-disable @typescript-eslint/no-explicit-any */
import { AppShell } from "@/components/app-shell";
import { requireRole } from "@/lib/auth";
import { createClient } from "@/lib/supabase/server";

function stockState(balance: number, reorder: number) {
  if (balance <= 0) return "Out of Stock";
  if (balance <= reorder) return "Near Reorder";
  return "In Stock";
}

export default async function InventoryPage({ searchParams }: { searchParams: Promise<{ error?: string; success?: string }> }) {
  const profile = await requireRole(["admin", "management_viewer"]);
  const query = await searchParams;
  const supabase = await createClient();
  const [{ data: items, error }, { data: issues }, { data: adjustments }] = await Promise.all([
    supabase.from("inventory_items").select("id,item_code,description,category,movement_category,balance_qty,reorder_level,cost,unit,is_active").order("movement_category").order("description"),
    supabase.from("inventory_issue_history").select("id,qty,balance_before,balance_after,issued_at,item:inventory_items(item_code,description),job:maintenance_jobs(job_no,room_no),staff:profiles!staff_id(full_name)").order("issued_at", { ascending: false }).limit(30),
    supabase.from("inventory_adjustment_history").select("id,adjustment_type,qty,balance_before,balance_after,note,adjusted_at,item:inventory_items(item_code,description),user:profiles!adjusted_by(full_name)").order("adjusted_at", { ascending: false }).limit(30),
  ]);
  const rows = items || [];
  const out = rows.filter((x: any) => Number(x.balance_qty) <= 0).length;
  const near = rows.filter((x: any) => Number(x.balance_qty) > 0 && Number(x.balance_qty) <= Number(x.reorder_level)).length;

  return <AppShell profile={profile} title="Inventory">
    <div className="section-head"><div><p className="eyebrow">Phase 4</p><h2>Inventory</h2><p className="subtle">Stock balance, movement category, reorder monitoring and movement history.</p></div></div>
    {query.error && <p className="error">{query.error}</p>}{query.success && <p className="success">Inventory saved successfully.</p>}{error && <p className="error">{error.message}</p>}
    <div className="metric-grid"><article className="metric"><span>Total Items</span><strong>{rows.length}</strong></article><article className="metric"><span>Near Reorder</span><strong>{near}</strong></article><article className="metric"><span>Out of Stock</span><strong>{out}</strong></article></div>
    {profile.role === "admin" && <section className="panel"><h3>Add Inventory Item</h3><form action="/admin/inventory/action" method="post" className="form-grid"><input type="hidden" name="action" value="create"/><label><span>Item Code</span><input name="itemCode" required/></label><label className="field-wide"><span>Description</span><input name="description" required/></label><label><span>Category</span><select name="category" required><option value="Building">Building</option><option value="Electrical">Electrical</option><option value="Painting">Painting</option><option value="Piping">Piping</option></select></label><label><span>Movement</span><select name="movement"><option value="fast">Fast Moving</option><option value="slow">Slow Moving</option><option value="once_in_a_while">Once in a while</option></select></label><label><span>Opening Balance</span><input name="balance" type="number" min="0" step="0.01" defaultValue="0" required/></label><label><span>Reorder Level</span><input name="reorder" type="number" min="0" step="0.01" defaultValue="0" required/></label><label><span>Unit</span><input name="unit" placeholder="pcs / box / tube"/></label><label><span>Cost (RM)</span><input name="cost" type="number" min="0" step="0.01"/></label><div className="field-wide actions"><button className="button" type="submit">Add Item</button></div></form></section>}
    <div className="section-head"><h2>Stock List</h2></div>
    <section className="panel list-panel">{rows.length === 0 ? <p className="subtle">No inventory items yet.</p> : rows.map((item: any) => { const state = stockState(Number(item.balance_qty), Number(item.reorder_level)); return <article className="list-row" key={item.id}><div className="field-wide"><div className="actions"><strong>{item.item_code} · {item.description}</strong><span className="badge">{state}</span>{!item.is_active && <span className="badge">Inactive</span>}</div><p>{item.category} · {String(item.movement_category).replaceAll("_", " ")} · Balance {item.balance_qty} {item.unit || ""} · Reorder {item.reorder_level}</p><small>{item.cost == null ? "Cost not entered" : `RM ${Number(item.cost).toFixed(2)}`}</small>{profile.role === "admin" && <details><summary className="text-link">Edit / Stock Adjustment</summary><div className="job-note-grid"><form action="/admin/inventory/action" method="post" className="panel form-grid"><input type="hidden" name="action" value="edit"/><input type="hidden" name="itemId" value={item.id}/><label className="field-wide"><span>Description</span><input name="description" defaultValue={item.description} required/></label><label><span>Category</span><select name="category" defaultValue={item.category}><option value="Building">Building</option><option value="Electrical">Electrical</option><option value="Painting">Painting</option><option value="Piping">Piping</option></select></label><label><span>Movement</span><select name="movement" defaultValue={item.movement_category}><option value="fast">Fast Moving</option><option value="slow">Slow Moving</option><option value="once_in_a_while">Once in a while</option></select></label><label><span>Reorder Level</span><input name="reorder" type="number" min="0" step="0.01" defaultValue={item.reorder_level} required/></label><label><span>Unit</span><input name="unit" defaultValue={item.unit || ""}/></label><label><span>Cost (RM)</span><input name="cost" type="number" min="0" step="0.01" defaultValue={item.cost ?? ""}/></label><label><span><input name="isActive" type="checkbox" defaultChecked={item.is_active}/> Active Item</span></label><div className="field-wide"><button className="button" type="submit">Save Details</button></div></form><form action="/admin/inventory/action" method="post" className="panel form-grid"><input type="hidden" name="action" value="adjust"/><input type="hidden" name="itemId" value={item.id}/><label><span>Adjustment</span><select name="adjustmentType"><option value="stock_in">Stock In</option><option value="correction_add">Correction Add</option><option value="correction_remove">Correction Remove</option></select></label><label><span>Quantity</span><input name="qty" type="number" min="0.01" step="0.01" required/></label><label className="field-wide"><span>Reason / Note</span><input name="note" placeholder="Invoice, delivery or correction reason" required/></label><div className="field-wide"><button className="button" type="submit">Update Stock</button></div></form></div></details>}</div></article>; })}</section>
    <div className="section-head"><h2>Material Issue / Usage History</h2></div>
    <section className="panel list-panel">{(issues || []).length === 0 ? <p className="subtle">No material issues yet.</p> : (issues || []).map((row: any) => <div className="list-row" key={row.id}><div><strong>{row.item?.item_code} · {row.item?.description}</strong><p>{row.job?.job_no} · {row.job?.room_no} · {row.staff?.full_name}</p><small>Issued {row.qty} · Balance {row.balance_before} → {row.balance_after}</small></div></div>)}</section>
    <div className="section-head"><h2>Stock Adjustment History</h2></div>
    <section className="panel list-panel">{(adjustments || []).length === 0 ? <p className="subtle">No stock adjustments yet.</p> : (adjustments || []).map((row: any) => <div className="list-row" key={row.id}><div><strong>{row.item?.item_code} · {row.item?.description}</strong><p>{String(row.adjustment_type).replaceAll("_", " ")} · {row.qty} · Balance {row.balance_before} → {row.balance_after}</p><small>{row.note} · {row.user?.full_name}</small></div></div>)}</section>
  </AppShell>;
}
