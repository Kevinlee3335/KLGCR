import Link from "next/link";import {AppShell} from "@/components/app-shell";import {SettingsForm} from "@/components/settings-form";import {requireRole} from "@/lib/auth";
export default async function SettingsPage(){const profile=await requireRole(["admin"]);return <AppShell profile={profile} title="Settings"><div className="section-head"><div><p className="eyebrow">Administration</p><h2>Settings</h2><p className="subtle">Manage organisation, integration and notification preferences.</p></div></div><div className="settings-grid"><SettingsForm/><aside className="panel settings-integration"><h3>Google Form Integration</h3><p className="subtle">The existing complaint import and webhook integration remains unchanged.</p><Link href="/admin/import">Open Google Import →</Link><h3>Notifications</h3><p className="subtle">Review current operational alerts and pending actions.</p><Link href="/admin/notifications">Open Notifications →</Link></aside></div></AppShell>}
