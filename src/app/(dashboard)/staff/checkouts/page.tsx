import Link from "next/link";import {AppShell} from "@/components/app-shell";import {requireRole} from "@/lib/auth";import {checkoutStatusLabel,type CheckoutRoom} from "@/lib/checkouts";import {createClient} from "@/lib/supabase/server";
export default async function Page(){const profile=await requireRole(["maintenance_staff","cleaner"]);const db=await createClient();const column=profile.role==="cleaner"?"cleaner_id":"assigned_to";const {data,error}=await db.from("checkout_rooms").select("id,reference_no,room_no,utmspace_defects,inspection_notes,status,assigned_to,cleaner_id,created_at,ready_at,block:blocks!block_id(code),assignee:profiles!assigned_to(full_name),cleaner:profiles!cleaner_id(full_name)").eq(column,profile.id).order("updated_at",{ascending:false});const rows=(data||[]) as unknown as CheckoutRoom[];return <AppShell profile={profile} title="Check-out Rooms"><div className="section-head"><div><p className="eyebrow">Room turnover</p><h2>{profile.role==="cleaner"?"Housekeeping handovers":"Assigned rectifications"}</h2><p className="subtle">A dedicated workflow, separate from resident maintenance complaints.</p></div></div><section className="panel list-panel">{error&&<p className="error">{error.message}</p>}<div className="mobile-cards" style={{display:"grid"}}>{rows.map(r=><Link className="panel record-card" href={`/staff/checkouts/${r.id}`} key={r.id}><div className="record-head"><strong>{r.reference_no}</strong><span className={`status-badge status-${r.status}`}>{checkoutStatusLabel[r.status]}</span></div><h3>Block {r.block?.code} · Room {r.room_no}</h3><p>{r.utmspace_defects}</p></Link>)}</div>{!rows.length&&<div className="empty"><strong>No rooms assigned</strong><span>New handovers will appear here.</span></div>}</section></AppShell>}
