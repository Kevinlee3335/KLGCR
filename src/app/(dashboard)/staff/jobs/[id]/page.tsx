import {notFound} from "next/navigation";import {AppShell} from "@/components/app-shell";import {PriorityBadge,StatusBadge} from "@/components/phase2-ui";import {requireRole} from "@/lib/auth";import {formatDate,type JobRow} from "@/lib/phase2";import {createClient} from "@/lib/supabase/server";import {startJob} from "../actions";
export default async function StaffJob({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string;started?:string}>}){const profile=await requireRole(["maintenance_staff"]),{id}=await params,q=await searchParams,s=await createClient();const {data}=await s.from("maintenance_jobs").select("id,job_no,room_no,category,description,priority,status,assigned_at,updated_at,started_at,completed_at,block:blocks!block_id(id,code),assignee:profiles!assigned_to(id,full_name),complaint:complaints!complaint_id(complaint_no)").eq("id",id).single();if(!data)notFound();const job=data as unknown as JobRow;return <AppShell profile={profile} title="Job Detail"><div className="section-head"><div><p className="eyebrow">{job.job_no}</p><h2>Block {job.block?.code} · {job.room_no}</h2><p className="subtle">Assigned {formatDate(job.assigned_at)}</p></div><div className="actions"><PriorityBadge value={job.priority}/><StatusBadge value={job.status}/></div></div>{q.error&&<p className="error">{q.error}</p>}{q.started&&<p className="success">Job started successfully.</p>}<section className="panel detail-grid"><div><span>Complaint</span><strong>{job.complaint?.complaint_no}</strong></div><div><span>Category</span><strong>{job.category}</strong></div><div className="field-wide"><span>Description</span><p>{job.description}</p></div></section>{job.status==="assigned"&&<form action={startJob.bind(null,id)} className="start-action"><button className="button">Start Job</button></form>}</AppShell>}
