import {describe,expect,it} from "vitest";import {readFileSync} from "node:fs";import {join} from "node:path";import {checkoutStatuses,checkoutStatusLabel} from "./checkouts";
describe("check-out room workflow",()=>{it("keeps turnover stages distinct and ordered",()=>{expect(checkoutStatuses).toEqual(["second_inspection","rectification","cleaning","verification","ready_for_occupancy"]);expect(checkoutStatusLabel.ready_for_occupancy).toBe("Ready for Occupancy")});it("ships tables, evidence storage and participant RLS",()=>{const sql=readFileSync(join(process.cwd(),"supabase/migrations/202609130001_checkout_room_workflow.sql"),"utf8");for(const token of ["checkout_rooms","checkout_defects","checkout_photos","checkout_history","checkout-evidence","enable row level security","assigned_to=auth.uid()","cleaner_id=auth.uid()"])expect(sql).toContain(token)})});
